import {createContext,useContext,useEffect,useState} from 'react';import api from '../services/api';
const C=createContext(null);export function AuthProvider({children}){const[user,setUser]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{api.get('/auth/me').then(r=>setUser(r.data.user)).catch(()=>{}).finally(()=>setLoading(false))},[]);const login=async data=>{const r=await api.post('/auth/login',data);localStorage.setItem('yg_token',r.data.token);setUser(r.data.user);return r.data.user};const register=async data=>{const r=await api.post('/auth/register',data);localStorage.setItem('yg_token',r.data.token);setUser(r.data.user);return r.data.user};const logout=()=>{localStorage.removeItem('yg_token');setUser(null)};return <C.Provider value={{user,loading,login,register,logout,setUser}}>{children}</C.Provider>};export const useAuth=()=>useContext(C);
