import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import Logo from '../components/Logo';import {useAuth} from '../context/AuthContext';
export default function Auth({mode='login'}){const isLogin=mode==='login';const[name,setName]=useState('');const[email,setEmail]=useState('');const[pw,setPw]=useState('');const[confirm,setConfirm]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const{login,register}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');if(!isLogin&&pw!==confirm)return setError('Passwords do not match');setBusy(true);try{const u=isLogin?await login({email,password:pw}):await register({name,email,password:pw});nav(isLogin?(u.onboardingComplete?'/dashboard':'/onboarding'):'/onboarding')}catch(err){setError(err.response?.data?.message||'Something went wrong')}finally{setBusy(false)}};return <div className="auth-page"><Link to="/"><Logo/></Link><div className="auth-card"><span className="eyebrow">{isLogin?'WELCOME BACK':'BEGIN YOUR JOURNEY'}</span><h1>{isLogin?'Welcome back':'Create your account'}</h1><p>{isLogin?'Pick up where you left off.':'A few details, then we personalize everything.'}</p><form onSubmit={submit}>{!isLogin&&<label>Name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label>}<label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required minLength="6" type="password" value={pw} onChange={e=>setPw(e.target.value)} placeholder="At least 6 characters"/></label>{!isLogin&&<label>Confirm Password<input required type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repeat your password"/></label>}{error&&<div className="error">{error}</div>}<button className="btn full" disabled={busy}>{busy?'Please wait…':isLogin?'Login':'Create Account'}</button></form>{isLogin&&<div className="auth-row"><label className="check"><input type="checkbox"/> Remember me</label><button className="link-button">Forgot password?</button></div>}<p className="switch">{isLogin?'New to YogaGenie?':'Already have an account?'} <Link to={isLogin?'/register':'/login'}>{isLogin?'Sign up':'Login'}</Link></p></div></div>}
