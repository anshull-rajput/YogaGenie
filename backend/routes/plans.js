import express from 'express';import {auth} from '../middleware/auth.js';import YogaPlan from '../models/YogaPlan.js';import {generatePlan} from '../services/planService.js';
const r=express.Router();r.use(auth);r.get('/',async(req,res)=>res.json({plan:await YogaPlan.findOne({userId:req.user._id,active:true}).populate('weeklySchedule.poses')}));r.get('/:id',async(req,res)=>res.json({plan:await YogaPlan.findOne({_id:req.params.id,userId:req.user._id})}));r.post('/generate',async(req,res)=>{try{await YogaPlan.updateMany({userId:req.user._id,active:true},{active:false});const p=await generatePlan(req.user);res.status(201).json({plan:p})}catch(e){res.status(500).json({message:'Could not generate plan'})}});export default r;
