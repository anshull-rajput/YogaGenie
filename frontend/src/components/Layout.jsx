import {NavLink,Outlet} from 'react-router-dom';import {Home,CalendarDays,Compass,BarChart3,MessageCircle,UserCircle,LogOut} from 'lucide-react';import Logo from './Logo';import {useAuth} from '../context/AuthContext';
const links=[['/dashboard','Dashboard',Home],['/plan','My Plan',CalendarDays],['/explore','Explore Yoga',Compass],['/progress','Progress',BarChart3],['/chat','Yoga Jenny',MessageCircle],['/profile','Profile',UserCircle]];
export default function Layout(){const{logout,user}=useAuth();return <div className="app-shell"><aside><Logo/><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to}><Icon size={18}/>{label}</NavLink>)}</nav><button className="logout" onClick={logout}><LogOut size={17}/>Sign out</button></aside><main className="app-main"><div className="mobile-head"><Logo/><span>{user?.name}</span></div><Outlet/></main><nav className="bottom-nav">{links.slice(0,5).map(([to,label,Icon])=><NavLink key={to} to={to}><Icon size={19}/><small>{label}</small></NavLink>)}</nav></div>}
