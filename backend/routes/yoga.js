import express from 'express';import YogaPose from '../models/YogaPose.js';
const r=express.Router();r.get('/poses',async(req,res)=>{try{const q={};if(req.query.goal)q.goals=req.query.goal;if(req.query.difficulty)q.difficulty=req.query.difficulty;if(req.query.style)q.style=req.query.style;if(req.query.bodyArea)q.targetAreas=req.query.bodyArea;const poses=await YogaPose.find(q).sort({name:1});res.json({poses})}catch(e){res.status(500).json({message:'Unable to load poses'})}});r.get('/poses/:id',async(req,res)=>{try{const p=await YogaPose.findById(req.params.id);if(!p)return res.status(404).json({message:'Pose not found'});res.json({pose:p})}catch(e){res.status(400).json({message:'Invalid pose id'})}});r.get('/routines',async(req,res)=>{const poses=await YogaPose.find(req.query.goal?{goals:req.query.goal}:{}).limit(30);res.json({routines:[{name:'Mindful Full Body Flow',goal:req.query.goal||'general-fitness',difficulty:req.query.difficulty||'beginner',duration:Number(req.query.duration)||20,poses}]} )});export default r;
