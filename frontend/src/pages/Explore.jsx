import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import api from '../services/api';
export default function Explore(){const[poses,setPoses]=useState([]);const[goal,setGoal]=useState('');const[difficulty,setDifficulty]=useState('');useEffect(()=>{api.get('/yoga/poses',{params:{goal,difficulty}}).then(r=>setPoses(r.data.poses||[]))},[goal,difficulty]);return <div className="page"><div className="page-title"><div><span className="eyebrow">EXPLORE YOGA</span><h1>Find your next pose</h1><p>A growing library of practices for different goals and levels.</p></div><Link className="btn btn-ghost" to="/chat">Ask Yoga Jenny</Link></div><div className="filters"><select value={goal} onChange={e=>setGoal(e.target.value)}><option value="">All goals</option><option value="flexibility">Flexibility</option><option value="strength">Strength</option><option value="posture">Posture</option><option value="stress-relief">Stress relief</option><option value="meditation">Meditation</option></select><select value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option value="">All levels</option><option value="beginner">Beginner</option><option value="intermediate">Intermediate</option><option value="advanced">Advanced</option></select></div><div className="pose-grid">{poses.map(p=><Link className="pose-card" to={`/pose/${p._id}`} key={p._id}><div className="pose-visual">🧘</div><div><span className="tag">{p.difficulty}</span><h3>{p.name}</h3><p>{p.sanskritName}</p><small>{p.duration} min · {p.targetAreas?.join(', ')}</small></div></Link>)}</div>{!poses.length&&<div className="empty">No poses match those filters yet.</div>}</div>}
