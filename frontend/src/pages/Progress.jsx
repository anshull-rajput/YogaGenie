import {useEffect,useState} from 'react';import {Flame,Clock3,CheckCircle2,CalendarDays} from 'lucide-react';import api from '../services/api';import '../progress.css';
export default function Progress(){const[p,setP]=useState();const[s,setS]=useState([]);const[error,setError]=useState('');useEffect(()=>{api.get('/progress').then(r=>{setP(r.data.progress);setS(r.data.sessions)}).catch(()=>setError('Unable to load your progress right now.'))},[]);const days=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'];const max=Math.max(1,...(p?.weeklyProgress||[]));return <div className="page"><div className="page-title"><div><span className="eyebrow">YOUR PROGRESS</span><h1>Small steps, lasting habits.</h1><p>Track consistency without chasing perfection.</p></div></div>{error&&<div className="error-banner" role="alert">{error}</div>}<div className="stats-grid"><div className="stat-card"><Flame/><span>Current streak</span><strong>{p?.currentStreak||0} days</strong></div><div className="stat-card"><Flame/><span>Longest streak</span><strong>{p?.longestStreak||0} days</strong></div><div className="stat-card"><CheckCircle2/><span>Sessions completed</span><strong>{p?.totalSessions||0}</strong></div><div className="stat-card"><Clock3/><span>Total practice</span><strong>{p?.totalMinutes||0} min</strong></div></div><section className="card progress-chart"><div className="card-head"><h2>Weekly practice</h2><span><CalendarDays size={15}/> Minutes</span></div><div className="chart-bars">{days.map((d,i)=><div className="chart-day" key={d}><div className="chart-track"><i style={{height:`${Math.round(((p?.weeklyProgress?.[i]||0)/max)*100)}%`}}/></div><b>{p?.weeklyProgress?.[i]||0}</b><small>{d}</small></div>)}</div></section><section className="card history"><h2>Practice history</h2>{s.length?s.map(x=><div className="history-row" key={x._id}><span><CheckCircle2 size={17}/> {x.day?`${x.day} practice`:'Session completed'}</span><b>{x.duration} min</b><small>{x.completedAt?new Date(x.completedAt).toLocaleDateString():'—'}</small></div>):<div className="empty">Complete your first session to start your progress history.</div>}</section></div>}
